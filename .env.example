# =============================================================================
# Camunda 7 Docker Compose – Environment Configuration
# =============================================================================
# Copy this file to .env and fill in the values before running:
#   cp .env.example .env
#   docker compose up -d
# =============================================================================

# -- PostgreSQL ---------------------------------------------------------------
POSTGRES_USER=camunda
POSTGRES_PASSWORD=changeme
POSTGRES_DB=camunda
POSTGRES_PORT=5432

# -- Camunda BPM Platform ----------------------------------------------------
CAMUNDA_VERSION=7.22.0
CAMUNDA_PORT=8080

# -- Database connection pool -------------------------------------------------
DB_CONN_MAXACTIVE=20
DB_CONN_MAXIDLE=20
DB_CONN_MINIDLE=5

# -- JVM settings -------------------------------------------------------------
JAVA_OPTS=-Xms512m -Xmx1024m

# -- Timezone -----------------------------------------------------------------
TZ=UTC

# -- Prometheus JMX Exporter (true/false) -------------------------------------
JMX_PROMETHEUS=false
CAMUNDA_JMX_PORT=9404

# -- pgAdmin (only used with --profile tools) ---------------------------------
PGADMIN_DEFAULT_EMAIL=admin@localhost
PGADMIN_PASSWORD=admin
PGADMIN_PORT=5050
